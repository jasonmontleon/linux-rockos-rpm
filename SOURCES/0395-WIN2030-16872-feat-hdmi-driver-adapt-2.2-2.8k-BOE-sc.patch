From 7a63a3a87780c11adf53c1c7e57af0ff135ad23a Mon Sep 17 00:00:00 2001
From: denglei <denglei@eswincomputing.com>
Date: Thu, 6 Feb 2025 14:40:35 +0800
Subject: [PATCH 395/416] WIN2030-16872:feat:hdmi driver adapt 2.2/2.8k BOE
 screen.

Changelogs:hdmi driver adapt 2.2/2.8k BOE screen.

Change-Id: I42e76108c57f767de281de54761a58aceef5a2e7
Signed-off-by: denglei <denglei@eswincomputing.com>
---
 drivers/gpu/drm/eswin/dw-hdmi.c    |  7 -------
 drivers/gpu/drm/eswin/es_dc.c      |  3 ++-
 drivers/gpu/drm/eswin/es_dw_hdmi.c | 24 ++++++++++++++++++++++++
 3 files changed, 26 insertions(+), 8 deletions(-)

diff --git a/drivers/gpu/drm/eswin/dw-hdmi.c b/drivers/gpu/drm/eswin/dw-hdmi.c
index 4a49bcfbd49f..dd244a700723 100644
--- a/drivers/gpu/drm/eswin/dw-hdmi.c
+++ b/drivers/gpu/drm/eswin/dw-hdmi.c
@@ -3371,13 +3371,6 @@ dw_hdmi_bridge_mode_valid(struct drm_bridge *bridge,
 	if (mode->flags & DRM_MODE_FLAG_INTERLACE)
 		return MODE_NO_INTERLACE;
 
-	if (mode->clock != 594000 && mode->clock != 297000 &&
-	    mode->clock != 148500 && mode->clock != 108000 &&
-	    mode->clock != 74250 && mode->clock != 54000 &&
-	    mode->clock != 27000 && mode->clock != 513820) {
-		return MODE_NOCLOCK;
-	}
-
 	if (pdata->mode_valid)
 		mode_status = pdata->mode_valid(hdmi, pdata->priv_data, info,
 						mode);
diff --git a/drivers/gpu/drm/eswin/es_dc.c b/drivers/gpu/drm/eswin/es_dc.c
index 80538ae9aa9e..7c608e29d25b 100644
--- a/drivers/gpu/drm/eswin/es_dc.c
+++ b/drivers/gpu/drm/eswin/es_dc.c
@@ -426,7 +426,7 @@ static bool es_dc_mode_fixup(struct device *dev,
 	struct es_dc *dc = dev_get_drvdata(dev);
 	long clk_rate;
 
-	dev_dbg(dev, "adjust mode clock:%d\n", adjusted_mode->clock);
+	dev_dbg(dev, "before adjust mode clock:%d\n", adjusted_mode->clock);
 	if (adjusted_mode->clock == 513820)
 		return true;
 
@@ -434,6 +434,7 @@ static bool es_dc_mode_fixup(struct device *dev,
 		clk_rate = clk_round_rate(dc->pix_clk,
 					  adjusted_mode->clock * 1000);
 		adjusted_mode->clock = DIV_ROUND_UP(clk_rate, 1000);
+		dev_dbg(dev, "adjust mode clock:%d\n", adjusted_mode->clock);
 	}
 
 	return true;
diff --git a/drivers/gpu/drm/eswin/es_dw_hdmi.c b/drivers/gpu/drm/eswin/es_dw_hdmi.c
index 84f75833aa62..eefbabba3c92 100644
--- a/drivers/gpu/drm/eswin/es_dw_hdmi.c
+++ b/drivers/gpu/drm/eswin/es_dw_hdmi.c
@@ -143,6 +143,14 @@ static const struct dw_hdmi_mpll_config eswin_mpll_cfg[] = {
 			{ 0x2008, 0x060f },
 		},
 	},
+	{
+		235690000,
+		{
+			{ 0x0000, 0x0605 },
+			{ 0x1018, 0x0219 },
+			{ 0x2648, 0x020f },
+		},
+	},
 	{
 		297000000,
 		{
@@ -151,6 +159,14 @@ static const struct dw_hdmi_mpll_config eswin_mpll_cfg[] = {
 			{ 0x2648, 0x020f },
 		},
 	},
+	{
+		371370000,
+		{
+			{ 0x0640, 0x0205 },
+			{ 0x1658, 0x0019 },
+			{ 0x2648, 0x000f },
+		},
+	},
 	{
 		513820000,
 		{
@@ -199,10 +215,18 @@ static const struct dw_hdmi_curr_ctrl eswin_cur_ctr[] = {
 		148500000,
 		{ 0x2080, 0x3203, 0x3141 },
 	},
+	{
+		235690000,
+		{ 0x3040, 0x3182, 0x3100 },
+	},
 	{
 		297000000,
 		{ 0x3041, 0x3182, 0x3100 },
 	},
+	{
+		371370000,
+		{ 0x3041, 0x31c0, 0x3100 },
+	},
 	{
 		513820000,
 		{ 0x3080, 0x31c0, 0x3100 },
-- 
2.47.0

